{
  "name": "aria-music-android",
  "version": "1.0.0",
  "private": true,
  "description": "Aria Music'in Android sürümü: servisleri Chrome Custom Tabs ile açan hafif bir kabuk.",
  "scripts": {
    "sync-services": "node scripts/sync-services.js",
    "sync": "npm run sync-services && cap sync android",
    "open": "cap open android",
    "build:debug": "npm run sync && cd android && ./gradlew assembleDebug",
    "build:release": "npm run sync && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/app-launcher": "^6.0.4",
    "@capacitor/browser": "^6.0.3",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
